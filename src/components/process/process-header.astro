---
import { processContent } from '../../constants/process'
import SectionHeader from '../shared/section-header.astro'
---

<SectionHeader
	eyebrow={processContent.eyebrow}
	heading={processContent.heading}
	subheading={processContent.subheading}
/>
